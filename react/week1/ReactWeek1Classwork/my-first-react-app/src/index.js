import React from "react";
import ReactDOM from "react-dom";
import Greeting from "./App";

const rootElement = document.getElementById("root");
ReactDOM.render(<Greeting name="Bikram Rawat" />, rootElement);
// Note : i have deleted the unnecessary files, folders and codes that are created by default using create-react-app command
